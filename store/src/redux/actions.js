export const ADD_TO_CART = (itemName) => ({
    type: "ADD_TO_CART",
    payload: itemName,
  });
  
  export const REMOVE_FROM_CART = (itemName) => ({
    type: "REMOVE_FROM_CART",
    payload: itemName,
  });
  